# Frontend environment template — copy to .env.local and fill in real values.
# DO NOT commit .env.local (it's gitignored).

# Backend API base URL (must end without trailing slash if not already /api suffixed).
NEXT_PUBLIC_API_URL=http://localhost:3001/api
